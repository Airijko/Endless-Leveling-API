package com.airijko.endlessleveling.enums;

/**
 * Represents which class assignment slot (primary or secondary) is being
 * modified.
 */
public enum ClassAssignmentSlot {
    PRIMARY,
    SECONDARY
}
