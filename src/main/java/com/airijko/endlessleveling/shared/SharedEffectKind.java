package com.airijko.endlessleveling.shared;

/**
 * Distinguishes where a shared effect comes from while keeping one common model.
 */
public enum SharedEffectKind {
    AUGMENT,
    ARCHETYPE_PASSIVE,
    INNATE_PASSIVE
}
